#Lista de paquetes base de la imagen
PACKS="vim curl iputils-ping net-tools nano wget procps"

#Comandos de puesta en marcha del entorno
COMMANDS="apt update,DEBIAN_FRONTEND=noninteractive apt install -y task-lxde-desktop xrdp,useradd -m -s /bin/bash tester,echo tester:abc123. | chpasswd,echo root:abc123. | chpasswd"

